version: '2'
services:
  db:
    image: mysql:5.7.17
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hettyversion
    ports:
      - 3306:3306
  app:
    build: .
    environment:
      HETTYVERSION_MYSQL_IP: 192.168.1.2
    ports:
      - 80:80
